"use client";

/**
 * P50/P80/P90, the probability of meeting the deadline, and every task's
 * criticality index.
 *
 * PLACEHOLDER — Phase 11 wave 3, Agent UI-ANALYSIS. Keep the prop signature.
 */
export default function ForecastPanel({ projectId }: { projectId: string }) {
  return (
    <p className="text-sm text-dim">Forecast is not built yet ({projectId}).</p>
  );
}
