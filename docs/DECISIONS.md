# Decisions

Judgement calls made during the autonomous refactor. One line of reasoning
each. Newest phase last.

Rule applied throughout: when ambiguous, choose the option that is **smaller,
more reversible, and closer to `docs/ARCHITECTURE.md`**.

---

## Phase 0

| # | Decision | Reasoning |
|---|---|---|
| D-01 | Keep the existing `./.venv` (Python 3.14.5) rather than creating a new one or pinning `backend/requirements.txt` exactly | Deps are already installed and the baseline suite is green (35/35). Re-pinning risks a dependency conflict for zero product value. Root `requirements.txt` pins *newer* versions than `backend/requirements.txt`; the installed venv matches root. I align `backend/requirements.txt` to what is installed rather than downgrading a working environment. |
| D-02 | Move `backend/app/core/config.py` → `backend/app/settings.py` and `backend/app/core/database.py` → `backend/app/db.py` | The prompt requires `core/` to be pure and enforced by an import test. Those two modules import `pydantic_settings` and `sqlalchemy`, so they cannot stay under `core/`. Moving two files is smaller than exempting them from the purity rule and weakening the invariant. Consistent with ARCHITECTURE A.1, which lists `db/` as a sibling of `core/`. |
| D-03 | Leave `backend/alembic/` inert; stay on `create_all` + a reset-and-seed command | `versions/` is empty — no migration has ever been generated, so there is no history to preserve. Both the prompt and ARCHITECTURE A.6 say explicitly not to spend hours on migrations. |
| D-04 | Stay on SQLite (`dwi.db`) | Prompt is explicit; ARCHITECTURE A.5 says the engine cannot tell the difference. `.env.example` mentioning Postgres is the thing that is wrong, so `.env.example` gets corrected, not `settings.py`. |
| D-05 | Purity is enforced by **AST source inspection**, not by import-time monkeypatching | Catches a forbidden import in a module that is never executed by any other test, and cannot be defeated by lazy imports inside functions (those are also parsed). |
| D-06 | Fix the non-hermetic `test_api.py` harness (module-import lifespan + shared on-disk `dwi.db`) **first**, in its own commit, before any schema change | It is the regression net for everything that follows. Repairing it while the schema is also moving would make failures unattributable. |
| D-07 | Migrate the seeded fixture as `effort == planned_duration` with exactly one assignee per task | Twelve existing assertions depend on its exact numbers (planned 22, projected 26, slip 4, 7 critical tasks, 4 bottlenecks). Keeping `effort/(1+0.6·(1−1))  = effort` makes the effort model a no-op for the fixture, so the regression net survives the rename. |
| D-08 | Delete root `scenario.py`, `api.py`, `index.html`; keep `demo.py` as a dev CLI under `backend/scripts/` | ARCHITECTURE A.6 marks all four DEMOTE and explicitly says keep `demo.py`, retire the rest once `core/` lands. Their behaviour is preserved: `scenario.py`'s data becomes a seed fixture, `api.py` is superseded by `backend/app/main.py`, `index.html` by the Next.js app. |
| D-09 | No conflict found between `docs/AUTONOMOUS_PROMPT.md` and `docs/ARCHITECTURE.md` in Phase 0 | The only near-conflict is persistence: ARCHITECTURE A.5 says "if Postgres already works, keep it", the prompt says stay on SQLite. The repo's working default *is* SQLite, so both point the same way. Recorded here because the prompt asked for conflicts to be logged. |

---

## Phase 1

| # | Decision | Reasoning |
|---|---|---|
| D-10 | `docs/SETUP.md` and `scripts/{setup,run-backend}.{sh,ps1}` appeared untracked in the working tree mid-run (timestamps 22:11, after this session started) and were swept into the phase-1 commit by `git add -A`. Kept, not reverted. | They are the repo owner's own onboarding scripts, they are correct, and they document the SQLite-only setup this refactor standardises on. Deleting them would destroy work that is not reproducible from seed code — an explicit hard-stop condition. From this point on every commit stages explicit paths rather than `-A`. |
| D-11 | `build_graph` sets `consumes: bool` and `dep_type` on every edge and drops the string `kind` attribute, rather than carrying both | ARCHITECTURE C says the artifact/temporal distinction becomes a `consumes` flag. Carrying both would be exactly the duplication the brief warns about. Cost is one mechanical line in the cycle test (`kind="temporal"` -> `consumes=False`); `stale_tasks` reads `consumes` and its four regression assertions are unchanged. |
| D-12 | `WorkflowSnapshot` stores sequences as tuples and wraps its derived lookups in `MappingProxyType` | Real immutability, not a convention. A frozen dataclass holding a plain `dict` is still mutable through the dict; the optimizer will evaluate thousands of candidates against one base snapshot and must not be able to corrupt it. |
| D-13 | A minimal `evaluate()` lands in Phase 1, not Phase 2 | The domain-leak test the brief requires in Phase 1 asserts byte-identical `evaluate()` output for two structurally identical workflows. The test cannot exist without the function. Phase 2 enriches it with the detector registry and tiering rather than introducing it. |
| D-14 | Task effort is stored as a single `effort` float with optional `optimistic/likely/pessimistic`; the seeded fixture sets `effort = planned_duration` and one assignee per task | Preserves the twelve assertions pinning the fixture's arithmetic (D-07) while giving Phase 3's effort model and Phase 4's three-point range real fields to read. |
| D-15 | `Resource.capacity` for the migrated fixture is derived the same way `_compute_dept_capacity` derived it (count of distinct people per group), and the five seeded groups become five `Resource` rows of `kind="team"` | Keeps `resource_contention` detecting exactly what `dept` contention detected, so the detector's regression assertions hold across the rename. The engine sees only `Resource{kind,name,capacity,skills}`; `kind` is data. |

| # | Decision | Reasoning |
|---|---|---|
| D-16 | Add `ResourceSpec.parent_key`, a roll-up parent, and measure contention against a resource capacity over the ready work of itself and its descendants | The seeded fixture names two marketing people but sets capacity 1, and that gap *is* the planted contention bottleneck. Assigning tasks to people alone loses the finding; assigning to teams alone loses the owner names; assigning to both makes every task look like it has two assignees and breaks the effort model. A parent keeps all three correct with exactly one assignment per task. Generic structure (teams, machine cells, budget rollups), not a domain concept -- so it does not reintroduce the leak. This supersedes the capacity-derivation claim in D-15, which was wrong: the seeded capacities are hand-authored and are preserved verbatim. |

---

## Phase 2

| # | Decision | Reasoning |
|---|---|---|
| D-17 | The registry is a function returning a tuple, not a decorator writing into a module-level list | `core/` holds no mutable module state, enforced by the purity test, and that is the precondition for evaluating optimizer candidates concurrently. A decorator would need a mutable accumulator. Adding a detector is still one line. |
| D-18 | Delete `accuracy.precision_vs_planted`; label **every** finding both fixtures contain and report `recall`, `precision`, and `planted_recall` separately | The prototype's metric punished the detectors for being right. Three planted faults were labelled; Phase 2 legitimately finds eleven real problems in that fixture, so `precision_vs_planted` would have read 33% while nothing was wrong. Labelling all eighteen findings across both fixtures (with a description each, and a `planted` flag preserving the original demo story) makes both numbers mean what they say, and turns the fixtures into a strict regression lock. |
| D-19 | A cyclic workflow makes `evaluate()` return `schedulable=False` plus a `dependency_cycle` finding, instead of raising `CycleError` | An exception is a worse answer than a finding: the UI can render the cycle, point at it, and stay up. `schedule()` still raises for direct callers, so the existing cycle-detection test is untouched. |
| D-20 | `Impact` carries `magnitude_kind` (`observed_delay_days` vs `exposed_days`) rather than reusing `attributed_delay_days` for Tier-0 findings | At Tier 0 nothing has been lost yet — the magnitude is work *exposed* to a structural weakness. Calling that a delay would be the quiet kind of dishonesty the whole brief is written against. Renaming the field is the smaller cost. |
| D-21 | `ready_but_idle` is declared Tier 2 (historical), not Tier 1 as ARCHITECTURE D.1's table has it | The tier is a *data requirement*, and this detector ages a task from when its last predecessor's status transition was recorded. With no event log that age is measured from day 0, so it would report every unstarted task on every fresh project. ARCHITECTURE D.1's table wins on design intent; this is a factual correction to which tier the detector needs. |
| D-22 | Add `projected_vs_planned_finish` (Tier 1) and `isolated_task` (Tier 0) beyond the prompt's explicit list | Both are in ARCHITECTURE D.1's table — "projected vs planned finish" and "orphan/unreachable tasks". The first is the headline number a delivery lead asks for, stated as a finding with its arithmetic attached rather than only as a top-level `slip_days`. |
| D-23 | `unavailable_checks` includes five `PLANNED_CHECKS` marked `(not built yet)` | Without them the Tier-3 gap lists nothing, which implies cross-project calibration becomes available once you have history. It does not — nothing collects cross-project actuals. Showing the roadmap in the payload is more honest than an empty gap. |
| D-24 | `zero_slack_chain` uses a configurable `critical_share_threshold`, default 0.6, rather than a hardcoded 0.5 | 50% of tasks being critical is common and unremarkable; my first threshold fired on a workflow with three tasks holding nine days of slack each. Two thirds is a plan with no shock absorber. Making it config, echoed in the evidence, means the number is reviewable rather than mine. |

---

## Phase 3

| # | Decision | Reasoning |
|---|---|---|
| D-25 | `inverse()` returns `tuple[Mutation, ...]`, not a single `Mutation` | Undoing a task removal is not one step: it re-adds the task, restores each edge with its `dep_type` and `consumes` flags, drops the bridges the removal created, re-attaches assignments, and re-links consumed requirements. A single-mutation inverse looked tidier and produced two silent round-trip failures. The signature should admit what is true. |
| D-26 | `TASK_DELAY_ADD` writes a separate `TaskSpec.added_delay` rather than raising `effort` | The fixture's stalled approval is nine elapsed days against a two-day estimate, so raising the estimate to seven changed the schedule not at all - the observed duration already exceeded it. A delay has to sit on top of what a task already looks like it will take. Keeping the two apart also means a slip reads as slip in `slip_days` rather than as a quietly re-baselined plan. |
| D-27 | Three mutations gained an optional "set" form (`RESOURCE_UNAVAILABLE_WINDOW.windows`, `TASK_DELAY_ADD.total_delay_days`, `REQUIREMENT_VERSION_BUMP.version_no`) instead of three new mutation kinds | Each exists only so the mutation's inverse is exact. Three extra kinds used by nothing but undo would take the algebra to twenty to save three optional fields, and would weaken the "seventeen kinds, closed" claim for no gain. |
| D-28 | `evaluate()` returns `schedulable=False` plus a `dependency_cycle` finding for a cyclic scenario, and `simulate()` returns the base evaluation plus the rejection for an invalid one, rather than raising | A UI needs to render "here is where you are, and here is why we will not do that". An exception gives it nothing to draw. |
| D-29 | `POST /projects/{id}/what-if` validates before writing anything and stores no row when `keep=false`; `POST /scenarios` stores an invalid scenario with `status="rejected"` and the reason | Two different needs. A throwaway question should leave no trace and must return the *structured* rejection - the cited constraint and the reason on record - because "M09 is mandatory" is much weaker than "M09 is mandatory because UN38.3 certification is a legal precondition to shipping". A deliberately created scenario should survive its own rejection so the user can read and edit it, which is also exactly what Phase 7 needs for LLM proposals. |
| D-30 | `to_canonical()` sorts set-like fields (`consumed_by`, `skills`, `required_skills`, `working_days`, `holidays`, `unavailable_windows`) | Their order carries no meaning, so two snapshots differing only in insertion order must hash the same. Found by a round-trip that produced an identical workflow with a different hash - which would have broken version comparison and the immutability guarantee, not just undo. |
| D-31 | `services/scenarios.get_row` uses `populate_existing=True` | The session is configured `expire_on_commit=False`, so an already-identity-mapped `Scenario` kept the `mutations` collection it was first loaded with and an appended mutation was invisible. A real bug, not a test artefact. |

---

## Phase 4

| # | Decision | Reasoning |
|---|---|---|
| D-32 | Risk is computed inside `evaluate()` rather than as a separate on-demand call | It is pure arithmetic over the schedule that call already produced, so it costs nothing extra, and it means one `analyze` round trip answers Capabilities 1 and 2 together. A separate call would have meant a second schedule pass for the same numbers. |
| D-33 | The `likely` run of the three-point range **is** the headline projection, not a separately computed estimate | Otherwise the middle of the range is a fourth number that does not match the one shown everywhere else, and the first question anyone asks is which of the two is real. |
| D-34 | `criticality_proximity` is rank-based, and `resource_pressure` measures the roll-up resource as well as the assignee | A pure slack ratio would duplicate `slack_ratio`; a rank distinguishes "third-tightest of seventeen" from "already critical". And a person with one task whose *team* is at capacity is genuinely under pressure - measuring only the person would miss the seeded marketing bottleneck entirely. |
| D-35 | The no-probability test asserts **substance, not vocabulary** | Banning the word "probability" is the wrong test once the payload is honest: it says "not a probability" four times, names a flag `is_probability`, and has a field called `what_would_make_this_a_probability`. A word-ban either fails on those denials or accumulates per-key exceptions until it proves nothing. The replacement walks the whole payload and fails if any key named like a likelihood holds a *number*, and separately checks that no sentence a user reads makes a probability claim. That catches the real failure mode. |
| D-36 | A factor that cannot be measured reports `available: false`, contributes 0, and is listed in the assumptions - rather than being imputed or silently dropped | Imputing it would invent evidence; dropping it would change the score's scale without saying so. Reporting zero with a reason keeps the arithmetic honest and tells the user what would unlock the factor, which is the same contract the detector tiering uses. |
| D-37 | Band thresholds (0.55 high, 0.30 moderate) are labels on a continuum and `band` is never returned without `score` | The cut points are arbitrary and admitting that is cheaper than defending them. Any UI that shows the band must show the number beside it. |

---

## Phase 5

| # | Decision | Reasoning |
|---|---|---|
| D-38 | Add `gen_drop_bottleneck_tasks`, a sixth generator, opt-in behind `aggressive=True` | The five deterministic generators are all restructurings, so none of them ever proposes deleting a task - which means the constraint gates never fire and the refusal beat is unreachable. An optimizer that never proposes the cheat never demonstrates that it will not take it. It is also the honest answer for a user who has marked nothing mandatory: cutting scope is a real option. |
| D-39 | The constraint gates follow task **lineage** through splits and merges | The first version refused `Split T02 across 2 people` with "this candidate drops T02 -> T03", which is false: the split rewires the edge to both parts and the ordering survives. A misleading refusal is worse than no refusal, because it teaches the user to distrust the ones that are real. A genuine deletion is still refused, and that is tested so the leniency cannot become a loophole. |
| D-40 | Return **two** recommendations: the top scorer, and the top scorer that does not change scope | `TASK_REMOVE` is on the brief's own justified list for the effort-conservation gate, so a permitted deletion can legitimately outrank every restructuring. Presenting only the winner would make "do less work" look like an engineering result. Presenting both makes it a scope decision, which is the user's to make and not the optimizer's. |
| D-41 | The time budget is a caller-supplied `should_stop` callable, not a clock read inside `core/` | Keeps the purity invariant literal - `core/` may not import `time` - and makes the budget test deterministic instead of a race against a real timer. The clock is already an argument everywhere else; this is the same rule. |
| D-42 | The base workflow is evaluated once and shared across candidates, rather than each candidate going through `simulate()` | `simulate()` evaluates base and candidate, so N candidates would cost 2N evaluations for N identical base results. Sharing it halves the work and is asserted by a test that counts the calls. The comparison payload is still `simulation._compare()`, so an optimizer candidate and a hand-written what-if produce the same shape. |
| D-43 | Surviving candidates are persisted as real `Scenario` rows by default | It is what lets the UI diff, edit or apply one without re-running the search, and it means there is no special apply path for an optimizer result - the existing scenario endpoints already work on it. `persist_candidates: false` turns it off for a throwaway comparison. |

---

## Phase 6

| # | Decision | Reasoning |
|---|---|---|
| D-44 | The open project and stage live in the **URL hash**, not in React state alone | Found in the browser: refreshing dropped the user back to the project list, losing their place. A hash is the smallest fix that also makes links deep-link, and Phase 9's "two browsers open the same project" needs it. Real routes are a later change if they earn one. The sync effect had to be gated on the initial restore finishing, because it fired on mount with no project and wiped the hash it was about to read. |
| D-45 | Delete `DashboardView`, `GanttChart`, `AccuracyPanel`, `DemoWalkthrough`, `TasksView`, `WhyLateView`, `BottleneckInbox`, `SimulationPanel`, `Header`, `TabNav`, `Card` rather than demote them into a hidden tab | The brief says demote the dashboard drift and keep an accuracy view reachable but off the landing surface. Keeping dead components around "just in case" is how drift returns. The accuracy view is rebuilt inside `VersionHistory`, where it belongs - it is evidence about the detectors, which is a history question. |
| D-46 | `DependencyGraph` colours nodes by **slack**, not by resource | The prototype keyed node colour off a hardcoded `{ORG, FIN, FAC, MKT, SPON}` palette - the domain leak in the frontend, and it would have shown grey for every node in any other domain. Slack is true of every workflow everywhere, and it is the thing the viewer actually needs to see. |
| D-47 | The what-if panel offers five **phrased questions** rather than a raw mutation editor | "A task slips" is what a delivery lead asks; `TASK_DELAY_ADD` is not. The algebra is not hidden - it is published at `/api/scenarios/mutation-kinds` and each queued change shows its `kind` as a badge - but a form that requires knowing the algebra to ask a question would make the closed algebra a burden rather than a guarantee. |
| D-48 | Frontend verification is two Playwright walkthroughs rather than component unit tests | The brief asks for the full journey completed in a browser for both seed domains. Component tests would re-check what the API tests already cover, and would not have caught either real bug this phase found - the lost-on-refresh state or the ambiguous button labels. Recorded risk: the walkthroughs currently live outside the repo. |

---

## Phase 7

| # | Decision | Reasoning |
|---|---|---|
| D-49 | `anthropic==1.4.0` is a **listed** requirement, not an optional extra, even though the default provider is `NullProvider` | An optional dependency that is never installed is a dependency that is never tested. Listing it means the SDK contract tests actually run, which is what catches a breaking SDK change before a demo does. The import stays lazy, so the application and the whole suite still work without it — the property the brief asks for is preserved without paying for it in coverage. |
| D-50 | Three roles, three prompts, three schemas — never one prompt that does all three | They fail differently and want different repair strategies: a bad interpretation is a misread sentence, a bad proposal is a wasted candidate, a bad narration is a false number. One mega-prompt makes every failure the same failure and gives the model authority over all three at once. |
| D-51 | The no-write-path guarantee is enforced by **parsing the AI modules**, not by a convention or a code review note | A convention holds until someone imports a service to "just look something up". The test reads every file under `ai/` and fails on a forbidden import or a `.commit()` call, so the guarantee survives the next person who does not know it exists. Same technique as the `core/` purity test, for the same reason. |
| D-52 | A narration containing a number the engine did not produce is **discarded**, not flagged | Flagging it puts the burden on the reader to notice a warning next to a confident sentence, which is exactly the failure this rule exists to prevent. Throwing it away and showing the engine's own wording costs a nicer paragraph and buys a guarantee that holds without the user's attention. The reason is still shown, because a visible discard is more trustworthy than a silent one. |
| D-53 | A numeric token glued to letters is an **identifier, not a quantity** — on both sides of the check | Found by the tests: `T03` in the payload made "3" an available number, so a narration claiming "3 days saved" would have passed verification. Task keys are everywhere in the payload, so nearly every small integer was silently licensed. Requiring the number to stand alone fixes the payload side; applying the same rule to the narration means "T03 is the bottleneck" still passes, which is the sentence a narrator actually wants to write. |
| D-54 | The projection has a **hard task ceiling** on top of the per-finding cap | Compaction was defeated by volume rather than by one wide finding: 300 isolated tasks produce 300 findings, each naming one task, and together they named everything. Capping findings considered (12) and tasks per finding (5) fixes that case; the ceiling makes it structural rather than a fix for one shape. The critical path is kept whole and the remaining slots go to the tightest slack, deterministically — so the prompt hash stays stable and the cache keeps working. |
| D-55 | The Proposer is a **third candidate source** in `services/optimization.py`, not a parallel path with its own scoring | Its proposals go through the same semantic validation, the same seven constraint gates and the same six-criterion scoring as the six deterministic generators. A model proposal that wins, wins on the same table everyone else is scored on; one that breaks a constraint comes back refused with the constraint cited. Giving the model its own path would make "the AI suggested it" a reason, and it is not one. |
| D-56 | With no model, the Interpreter falls back to a **labelled pattern matcher** rather than a flat refusal | A flat refusal is simpler and would have been defensible. But the demo has to run with the LLM disabled, and "type a sentence" that always fails is worse than no box at all. The matcher handles the same four shapes the what-if form offers and refuses everything else, and both the API (`method`) and the UI ("matched by pattern (no model configured)") say what produced the answer. The rule it must not break — never guess — is intact; it just does its guessing in public. |

---

## Phase 8

| # | Decision | Reasoning |
|---|---|---|
| D-57 | Add a tenth Tier-0 detector, `critical_path_single_owner`, in Phase 8 rather than noting the gap and moving on | The demo walk found it: ARCHITECTURE Section I's cold-start beat names "one person on three zero-slack tasks", and no detector said that. `resource_overallocated` structurally cannot - a strictly sequential chain never asks for the resource in two places at once, so capacity is never exceeded and the plan looks fine. It is not fine: a critical path owned end to end by one person has no redundancy at all. The detector defers to `unassigned_critical_task` when somebody is missing, and stays quiet for a team resource with more than one member, because a team can absorb an absence and a person cannot. |
| D-58 | The risk score is serialised as the sum of the **rounded** contributions, not as the rounded sum | Found by running the demo walk twice: the factor column added up to 0.6298 while the headline read 0.6299. The product's claim is "add the column up yourself" - a reader who does that and gets a different number has caught the system being loose with arithmetic, and every other honest number on the screen becomes suspect. Rounding once, at the point of display, costs at most half a thousandth of ranking precision and makes what is on screen exact. |
| D-59 | The "LLM enabled" rehearsal runs against a **recorded provider**, not a live API | No `ANTHROPIC_API_KEY` exists in the repository or the environment, and the brief names a missing credential as a stop condition. Halting the whole run over it would be wrong - the credential is needed for one *verification*, not for the product, which is designed to work without it. So the enabled path is exercised end to end with replayed structured output: the plumbing, the validation, the gating, the caching and the number check all run for real. What has never run is a live model call, and that is stated as a risk rather than papered over. |
| D-60 | `demo_check` asserts what each beat **claims**, not that each endpoint returns 200 | A smoke test that walks the same path would have passed while the cold-start beat had nothing to say about the single owner, while the factor column did not add up, and while a stale narration number went uncaught. Each of those was found by asserting the sentence the presenter says out loud. The script is the rehearsal, in a form that fails. |

---

## Phase 9

| # | Decision | Reasoning |
|---|---|---|
| D-61 | `CORS_ORIGINS` accepts a **comma-separated list**, and `*` is refused at startup | A hosting dashboard gives you a single-line text box; quoting a JSON array in one is how you end up with a deployment that has no CORS and a frontend that renders an empty page. The JSON form still parses, so an existing deployment does not break. `*` is refused rather than accepted because this API allows credentials, and a wildcard with credentials is rejected by every browser - accepting it would produce something that looks configured and behaves like a blocklist. |
| D-62 | `Annotated[list[str], NoDecode]` on `CORS_ORIGINS` | Found by testing the thing I had just written: pydantic-settings JSON-decodes complex fields from the environment *before* any validator runs, so the comma-separated form died with a `JSONDecodeError` at import time - the application would not start at all. `NoDecode` hands the raw string to the validator. |
| D-63 | Structured errors pass the **detail through untouched** rather than flattening it to a string | `{rejections: [{constraint, constraint_reason, ...}]}` and `{cycles: [...]}` are the product. A middleware that turned those into `"detail": "mutation 1: ..."` would throw away the only part of a refusal a user can act on, which is exactly the thing this system is built to preserve. The wrapper adds `error`, `hint` and `request_id` around it and touches nothing inside. |
| D-64 | `/health` does **not** touch the database; `/ready` does | They answer different questions and platforms use them differently. A liveness probe that fails when the database blips gets the container killed and restarted, turning a recoverable outage into a crash loop. Splitting them also makes the most common deployment failure self-diagnosing: `/health` passing and `/ready` failing means `DATABASE_URL` is wrong, and `docs/DEPLOY.md` says so in those words. |
| D-65 | The request deadline is a **backstop**, and the optimizer's real budget stays the injected `should_stop` | `asyncio.wait_for` cannot interrupt synchronous CPU work; it frees the request and the computation finishes on its own. That is acceptable for pure, finite, lock-free work, and it is honest to say so in the module rather than imply a hard kill. The mechanism that should actually fire is the one inside the search, which stops between candidates and returns the partial ranking it already has - measured at 11 ranked candidates in 136ms against a 0.1s budget. |
| D-66 | The picked identity is sent as `X-User-Id` and **never checked** | It drives `created_by` and the member list, and nothing else. A missing, malformed or stale id is served identically to a valid one, because refusing a request over it would be enforcing an identity - which is authentication, which is explicitly out of scope. A test asserts that `/api/login`, `/api/auth` and friends do not exist, so adding one has to be a deliberate act. |
| D-67 | `POST /admin/reset-seed` is **disabled** when no `ADMIN_TOKEN` is set, rather than open | An unguarded database reset on a public URL is an undo button for your demo that anyone on the internet can press. Disabled-by-default means the safe configuration is the one you get by doing nothing. The comparison is `hmac.compare_digest`, because comparing a secret with `==` leaks its length and prefix to anyone patient enough to measure. |
| D-68 | Every stage gets **its own** error boundary, keyed on the stage | One boundary around the whole app would mean a bug in the dependency graph blanks the findings above it. Per-panel boundaries cost the user the panel and not the session, and the graph gets its own because it is the only thing on screen with a third-party layout engine underneath it. Keying on the stage clears a failure when the user moves on, so one broken panel cannot poison the nav until a reload. |
| D-69 | The walkthroughs move into `frontend/e2e/` with `playwright` as a devDependency | Carried from Phase 6's recorded risk: verification that lives in a scratch directory is verification nobody else can run. They also needed a shared `signIn` helper the moment the name picker became the first screen - which is itself the argument for keeping them in the repo, where a product change breaks them visibly instead of silently. |
| D-70 | The frontend gets its own `.dockerignore` | Its build context is `./frontend`, so the repo-root file does not apply. Without it, `npm ci` copies a host-built `node_modules` into the image and the build fails in a way that looks like a dependency problem and is not. |
| D-71 | The optimizer's spinner names the stage on a **timer**, and the timer is deliberately slower than the search | The search is one request, so the client genuinely cannot know which stage the server is in. Advancing labels every 700ms against a search that finishes in about 300ms means the label never claims to be further along than it could be - it stops on the last stage and waits. The alternative, streaming real progress, would mean a websocket for a third of a second of work. |
| D-72 | `docs/DEPLOY.md` supersedes the untracked `deploy-kit/` draft, which is left in place rather than deleted | The draft was user-supplied and mostly right; two things in it were stale (a `COPY engine.py` that Phase 1 removed, and `CORS_ORIGINS` documented as a JSON array, which is now the secondary form). It is untracked working material that is not mine to delete, so it stays and `docs/DEPLOY.md` is the one the repository points at. |

---

## Phase 10 — authentication

Phase 9 shipped an identity nobody checked and said so plainly (D-66). This
phase replaces it with a Google session and makes `ProjectMember.role`
enforced. The one architectural idea everything else follows from: the browser
only ever talks to the Next.js origin, so Auth.js runs in Next, the session
cookie is same-origin, and Next injects identity into the upstream request.

| # | Decision | Reasoning |
|---|---|---|
| D-73 | Identity is injected in `src/proxy.ts` via `NextResponse.rewrite(upstreamURL, { request: { headers } })`, and the `/api/:path*` rewrite is **deleted** from `next.config.ts` rather than kept as a fallback | `rewrites()` is static configuration: it runs per request but cannot read the session cookie, so it can never say who is asking. The proxy already has to run for page protection, and the rewrite hands the request to Next's own streaming proxy, so method, body and response pass through verbatim for free — a route handler would mean hand-rolling `duplex: 'half'`, method fan-out and header filtering. Keeping the config rewrite as a fallback would have been worse than deleting it: it is precisely the path a request takes when it slips past the matcher, and it reaches FastAPI with no identity and no secret. One door is the point. |
| D-74 | The file is `src/proxy.ts`, not `middleware.ts`, and not at the project root | Next 16 deprecated the `middleware` convention and renamed it `proxy`, function name included; `middleware.ts` still works but warns, and having both is a hard build error. The location is not cosmetic: Next scans only the directory beside `app`, so a root `proxy.ts` under a `src/app` layout is **silently ignored** — the build succeeds, `middleware-manifest.json` comes out empty, and nothing is protected. Found by reading the manifest, because nothing warns. |
| D-75 | The proxy matcher is two entries: a bare `/api/:path*`, and a negative-lookahead pattern for everything else | The matcher every tutorial copies begins with a negative lookahead on `api`, which removes `/api/*` from the proxy and leaves identity injection nowhere to happen. And the asset-extension exclusions the page pattern needs would punch a `/api/anything.png` hole straight through to the backend. Listing `/api` separately, with no exclusions, closes both. |
| D-76 | Both `X-User-Id` and `X-Proxy-Secret` are deleted from every incoming request before ours are set — including when no secret is configured, and including on page requests | This is the entire security property. A browser that sends its own `X-Proxy-Secret` and gets it forwarded makes the design *worse* than no auth, because the backend would then trust it. Next's `x-middleware-override-headers` replaces the header set rather than merging it, so anything absent from the forwarded `Headers` is dropped upstream. Asserted against a real build with a spoofing client, not reasoned about. |
| D-77 | Sessions are JWTs with **no database adapter** | The backend already owns user rows; a Next-side copy would be a second source of truth about who someone is. It also lets the proxy decide whether a request is authenticated without a database round trip on every prefetch, which matters because the proxy runs on every route. |
| D-78 | A failed backend upsert **fails the sign-in** (`?error=AccessDenied`, no cookie minted), with a self-healing retry in the `jwt` callback as a second layer | The failure mode to avoid is a session with no backend id: the user sees a logged-in app while every request they make is anonymous, and everything they create belongs to nobody. Failing at sign-in is the only point where there is a page to put a readable sentence on. `AccessDenied` specifically, because Auth.js forwards only a fixed allow-list of error codes to the client and a custom one degrades to "Configuration". |
| D-79 | The walkthrough Credentials provider is gated on `E2E_AUTH_ENABLED=1` **and** `NODE_ENV !== "production"`, and is not constructed at all when either fails | Google's consent screen cannot be driven headless, so `npm run e2e:all` needs a door — but a password-free sign-in a deployment can be talked into using is not a door, it is a hole. Because Next inlines `NODE_ENV` at build time, the guard is statically false in any `next build` output: the provider *cannot exist* in a production bundle, which is stronger than checking at runtime. The accepted cost is that the walkthroughs must run against `next dev`, which is what `e2e/README.md` already prescribed. |
| D-80 | Auth.js's own endpoints stay mounted at `/api/auth/**` rather than moving to a non-`/api` prefix | Keeping the default `basePath` means `NEXTAUTH_URL`, the callback URLs registered with Google, and every Auth.js client helper all work unconfigured. The proxy exempts the prefix explicitly and filesystem routes are matched before `afterFiles` rewrites, so it never reaches FastAPI — verified: `/api/auth/nope` returns Auth.js's own 400, not a proxied 404. |
| D-81 | `backendUserId` is declared by augmenting `@auth/core/types` and `@auth/core/jwt`, not `"next-auth"` | The Auth.js docs say to augment `"next-auth"`, but `next-auth` only *re-exports* those interfaces. Declaring them under `"next-auth"` creates a second, shadowing interface that Auth.js's own callback signatures never see — the field would type-check at the call site and be missing everywhere it actually matters. |
| D-82 | The API trusts a **header pair**, `X-User-Id` plus `X-Proxy-Secret`, and nothing else. **Supersedes D-66.** | D-66 was right for a name picker on a shared demo and wrong the moment a real session exists: an identity nobody checks is fine when nothing is protected, and is impersonation-as-a-feature when a role is. Putting the login in FastAPI would have meant sessions, callbacks and a second place to get CSRF wrong, next to the framework that already does it; putting it in front means this service verifies one secret and keeps its whole surface stateless. Rejected: verifying a JWT here — it moves the same trust decision into key rotation and clock skew for no gain, because the proxy is the only thing that can reach the API anyway. `hmac.compare_digest`, for D-67's reason. There is still no login route in FastAPI, and the test asserting so stays green. |
| D-83 | `PROXY_SHARED_SECRET` **unset means the pre-auth behaviour, unchanged** — the header trusted as sent, no role enforced, anonymous served | The 780 tests that existed before this phase are all written against the open instance, and so is a fresh clone with no `.env`. Treating the closed configuration as the default would have meant editing hundreds of tests to add a header, which is how a permission layer gets merged with its own regression net rewritten to agree with it. One switch, off by default, means the compatibility path *is* the default path and is exercised on every single run rather than remembered. |
| D-84 | Enforcement is **one router-level dependency that refuses by default**, not a decorator per write route | A per-route `Depends(require_editor)` is one commit away from a new write endpoint nobody annotated, and that failure is silent: the endpoint works, for everybody, and no test fails. So the guard is attached to the router, everything that is not a `GET` needs `editor`, and the only escape is an explicit `(method, path)` entry in `deps.READS_THAT_POST`. Adding a write inherits the guard; exempting a read is a reviewable line in one file. Two tests walk every route in the application and fail on an unguarded write or a stale exemption — in the spirit of `test_core_purity`, and they had to recurse through FastAPI 0.141's lazy router inclusion, because `app.routes` is no longer flat and a test that iterated it would quietly assert nothing. |
| D-85 | A role protects **authorship, not visibility**: reads stay open to anonymous and non-member requests alike | This product has never been multi-tenant. `GET /api/projects` lists every project to everybody and a Phase 9 test asserts two people see the same project, so member-only reads would either break that test or contradict the endpoint next to it. The rejected alternative — 403 on read for a non-member — is a tenancy model, not a permission fix, and it would break "send a colleague the link", which is what the sharing story is for. What is protected is the thing that was actually at risk: a stranger changing your plan. |
| D-86 | A **viewer may evaluate, optimize and ask a what-if**; the line is "does it move `project.current_version_id`" | A read-only seat that cannot ask "what happens if this slips" is a screenshot, and Capability 3 is the product. Drawing the line at HTTP method would have refused `POST /analyze`; drawing it at "writes any row" would have refused `evaluate`, which writes an `AnalysisRun`, and `optimize`, which writes candidate `Scenario` rows. Both are scratch paper — checked, not assumed: no service behind analyze, risk, optimize, interpret or explain contains `write_version`, `ensure_draft` or an assignment to `current_version_id`. `apply` is the one endpoint that does, and it is squarely a write. |
| D-87 | `POST /api/seed/reset` inherits `ADMIN_TOKEN` the moment a deployment authenticates | It is `POST /admin/reset-seed` without the token — the same total destruction through a second door, and it predates the guarded one. Disabling it outright would break the local reset `docs/PROGRESS.md` sends developers to, so it follows the same one switch as everything else: untouched with no `PROXY_SHARED_SECRET`, behind D-67's token once there is one. Merely being signed in is deliberately not enough — no ordinary user should be able to drop every project on the instance. |
| D-88 | `NEXT_PUBLIC_API_URL` is **removed from the codebase**, not documented as "must be empty". `src/lib/api.ts` hard-codes a relative base | Found during integration, and it would have made the whole phase decorative. `api.ts` read that variable as its base URL, and `docker-compose.yml` and `frontend/Dockerfile` both *defaulted* it to an absolute backend URL. With it set, the browser calls FastAPI directly, cross-origin — past the Next origin, past the session check, past the header injection — and the app looks signed-in while every request arrives anonymous. Documenting "leave this blank" would have made the safe configuration the one you get by remembering. Deleting the variable makes it the only configuration there is. The backend URL is now server-side only (`API_REWRITE_URL`), which also means re-pointing the frontend is a restart rather than a rebuild. |
| D-89 | The proxy sends `X-User-Id` **unconditionally** and only `X-Proxy-Secret` conditionally | The two headers are not symmetrical and the first draft treated them as one, sending neither unless a secret was configured. Both auth agents independently flagged the consequence: the backend still trusts a bare `X-User-Id` in open mode, so withholding the identity there protects nothing and instead makes `created_by` come back null with no member row — ownership silently disappearing in exactly the configuration a developer runs locally. The session has already been verified by the time the header is set; the secret's only job is to prove to the backend that the header came from us, and where the backend is not asking for that proof there is nothing to withhold. |
| D-90 | `backend/tests/conftest.py` pins `PROXY_SHARED_SECRET=""` before the app is imported | Same technique and same reason as the existing `DATABASE_URL` pin (D-06). `Settings` reads `env_file=".env"`, so a developer who put a real secret in the repo-root `.env` would silently flip the entire suite into enforcing mode, and the tests asserting the open behaviour would fail on their machine and nowhere else. An explicitly empty environment variable outranks the dotenv value — verified — so the compatibility path is the one the suite always runs, and `test_auth.py` sets the secret per-test on the live `settings` object to cover the other half. |
| D-91 | The name picker is **deleted**, and the walkthrough asserts its honesty copy is *gone* rather than just dropping the assertion | `WhoAreYou.tsx` promised "there is no login here", "no password needed" and "anyone with this link can pick any name". Every word was true and every word is now false, so leaving the component as a fallback would have left a second, unenforced identity next to the real one — a user could name themselves twice and the second would be ignored. The browser walkthrough now asserts that copy is absent, so a revert cannot quietly restore a false promise. What replaces it is a claim the picker could never make, and which the walkthrough also asserts: a signed-out request to `/api/*` is refused with a 401 in the standard envelope. |

## Phase 10 — the design system

| # | Decision | Reasoning |
|---|---|---|
| D-92 | Base `radix`, style `nova`, via `npx shadcn@latest init --base radix --preset nova` | The task requires `sonner`, and the skill's own rule is that Base UI projects use the `toast` component while Radix projects use `sonner` — choosing `base` would have made the required toast library the wrong one for the project. Radix's `asChild` is also the composition idiom the wave-2 agents are most likely to already know. |
| D-93 | Legacy `--color-accent` keeps the brand blue; the shadcn `accent` role loses to it, and `select.tsx`'s focus highlight moves to `muted` | The two vocabularies collide on exactly one name: legacy `accent` is a saturated blue used as `text-accent` and `bg-accent text-background`; shadcn `accent` is a subtle hover surface. Only one can own `bg-accent`. Letting shadcn win would have turned every `text-accent` near-invisible and every primary button grey-on-dark across twelve panels — a silent mis-render, not a build failure. Letting legacy win costs one line in `select.tsx`, the only one of the fourteen primitives that referenced `accent` at all. Rejected: renaming the legacy token, which means editing the twelve panels wave 1 is forbidden to touch. |
| D-94 | `--primary` is a strong neutral, not the accent | The brief reserves one accent for critical path and high-severity findings. If `primary` were the blue, every default shadcn button would be blue and the reservation would be meaningless within a day. Muted neutrals carry actions; the accent carries meaning. |
| D-95 | Dark mode is a `@media (prefers-color-scheme: dark)` branch on `:root:not(.light)` **in addition to** shadcn's `.dark` class variant, with `@custom-variant dark` rewritten to match both | shadcn ships class-only dark mode and documents `next-themes` as the switch, which needs a provider in `layout.tsx` — a file that agent did not own, and a provider would be a feature the brief excludes. Class-only would have rendered the app light-on-light regardless of the user's system preference, which is the exact catastrophic regression wave 1 had to avoid. Keeping both branches means a future `next-themes` toggle drops in and wins without touching the CSS again. |
| D-96 | Derived aliases (`--card`, `--muted`, `--red`, and the rest) are declared on `:root, .light, .dark` rather than once on `:root` | A custom property whose value contains `var()` is substituted at computed-value time on the element that declares it. Declared only on `:root`, `--card: var(--panel)` would resolve against `:root`'s raw values, and a `.dark` class placed on anything but the root element would inherit an already-substituted light colour. Repeating the alias block on every mode-bearing selector costs nothing and removes a failure mode nobody would find by reading the file. |
| D-97 | Severity is three named tokens — `severity-high`, `severity-medium`, `severity-low` — set to the same values as the existing `red`/`amber`/`green`, which survive as aliases | Naming the states rather than the hues is what makes a fourth one hard to add: there is no `severity-info` to reach for. Setting them to the existing values makes wave 2's migration a rename with no visual diff, so a mis-mapped severity shows up as a wrong word rather than a wrong colour. `violet` stays defined because `OptimizePanel` still names it, and is documented as not a severity. Rejected: deleting `violet`, which breaks a component wave 1 may not edit. |
| D-98 | Tabular numerals are a base rule on `body`, not a utility wave 2 must apply | The brief asks for tabular figures everywhere a number appears; in this app that is everywhere. A utility is something each agent has to remember on each cell, and the failure mode is a column that *almost* lines up. One base rule, with Tailwind's built-in `proportional-nums` as the escape hatch for running prose, gets it right by default. |
| D-99 | The React Flow overrides now read the raw tokens (`var(--panel)`) instead of Tailwind's theme aliases (`var(--color-panel)`) | Not cosmetic, and it fixes a pre-existing bug: `@theme inline` emits **no** CSS custom properties, so the compiled `:root` contains no `--color-*` at all. The overrides referenced `var(--color-panel)` and `var(--color-background)` and were therefore already dead on `main`, silently — the page background was in fact being carried by the `bg-background` utility on the body element. Reading the raw tokens makes them work, and makes them follow light mode for free. Anyone writing plain CSS against these tokens must use the raw names. |
| D-100 | Light values are Primer's light semantic set, paired with the existing dark values, which are Primer's dark set | The twelve dark values already in the file are GitHub's. Taking the light halves of the same system means the two modes are siblings rather than one being a guess at inverting the other, and every foreground token clears 4.5:1 against both `background` and `panel` in both modes (lowest is 4.74:1). In light the surface hierarchy inverts — tinted canvas, white panels — so a card still lifts off the page instead of vanishing. |

---

## Phase 10 — wave 2, the visual overhaul

The instruction was to fix a UI that reads as generated, *mostly by subtraction*.
What came out is measurable: the analysis stage went from eleven bordered cards
to one screen of dense rows, `page.tsx` lost its equal-weight tile grid, the
dependency map became a time-and-resource chart instead of a node soup, and
`npm run lint` went from four problems to none.

| # | Decision | Reasoning |
|---|---|---|
| D-101 | The diff is **one table with real `Before` / `After` / `Delta` columns**, one row per measurement, not two stacked lists or a column of arrows | Capability 3 is judged on whether a reader can see what moved. Stacked lists make the reader hold a number in their head and walk down to find its pair; arrows put the comparison inside a cell where the eye cannot scan a column. Seven measurements — completion, deadline margin, verdict, open findings, zero-slack chain, resources over capacity, total effort — now line up under one delta column with tabular figures. Changed rows use the `Table` primitive's own `data-state="selected"` highlight and unchanged rows go dim with an em-dash, so "nothing moved" is legible at a glance rather than being seven identical-looking rows. Rejected: two cards side by side, which is the arrangement the brief calls the generated look and which cannot align a delta at all. |
| D-102 | `violet` is removed from the four change panels; a **constraint on record** is distinguished by *form* — a bordered mono chip — not by hue | `violet` marked `MANDATORY_TASK` and the refusal frames. A constraint is a fact, not a severity, but a fourth saturated hue in a palette with exactly three severity states invites the reader to rank it against them, which makes it a fourth severity in disguise. Identifiers already have a visual language here — monospace — so the chip carries the "on record" reading with no colour spent, and the refusal gets its weight from a heading, a glyph and first position instead. Rejected: mapping `violet` onto `severity-medium`, which would have said "a legal precondition is a medium-severity problem". |
| D-103 | The optimizer's staged-search label resets in the **event handler**, not in the effect's idle branch | `react-hooks/set-state-in-effect` was flagging `setPhase(0)` inside the effect body, and it was right: a cascading render for a value nothing was reading, because the label is not rendered when the search is idle. Moving the reset to the top of `run()` puts it in the same batch as `setBusy(true)`, so every run still starts on the first label and the effect's only job is the interval it owns. D-71's honesty property is untouched — the labels still advance on a timer slower than the search and still stop on the last one rather than claiming completion. |
| D-104 | Native `<select>` and native `<input type="checkbox">` are **kept** in the what-if composer and the optimizer budget, and commented as load-bearing | A shadcn `Select` renders a button and a portalled listbox, which `page.locator("select").nth(1).selectOption({ index: 1 })` cannot drive; `journey.mjs` indexes into these controls by DOM position, so replacing them would have traded a working walkthrough for a nicer dropdown. Styling the native control to match the shadcn `Input` beside it gets the whole visual benefit at zero cost. The comment says why, because the next agent's instinct will be to "finish the migration". |
| D-105 | "Why the total is not the answer" comes **out of its disclosure**; "nothing is written" comes out of small caps | Both were quiet in a way that worked against them. The first is the honest counterweight to a single blended score and it was one click away from never being read; it now sits under the comparison table permanently. The second is the what-if panel's integrity claim and it was rendered like a column header, which reads as decoration rather than as a sentence someone means. Small caps are now reserved for table heads and metric labels. Nothing moved in the other direction: no disclaimer was shortened, softened or put behind a disclosure anywhere in this wave. |
| D-106 | A finding is a **row on a ruled cause heading**, not a card. The only rule inside the findings list is a hairline between rows; severity is a 2px left rule and the impact arithmetic sits on the finding's own title line | The panel had three levels of border around one sentence — a card per cause, a bordered "Do this:" block inside it, a bordered impact row inside that — and eleven findings meant eleven boxes of equal visual weight, which is exactly the generated look the brief exists to remove. Typography and one hairline carry the same hierarchy in a third of the vertical space, so all eleven findings *and* their arithmetic fit on one screen instead of four. Rejected: keeping the card and tightening its padding, which shrinks the symptom and not the cause. |
| D-107 | In the analysis panels the accent is spent on **one** thing: a `critical path` marker on a finding whose task is on the zero-slack chain | The old panel spent the accent on the cause key, on every disclosure trigger and on the risk bar — at which point it marks nothing. Reserving it means it answers a question ("does this one move the finish date?") rather than decorating. The cost is honest and visible: on the seeded symposium 8 of 11 findings genuinely are on the critical path, so the accent is busy there — but it is busy because the workflow is, not because the UI is. |
| D-108 | The nine-factor table is a **permanently open block** that the ranked list re-points, replacing the per-row accordion; the score is one small figure on the table's heading line | The brief's instruction is that the decomposition *is* the product and must not hide behind a single score, and an accordion whose closed state is the default hides it behind exactly that. The state is the same `useState` with the same default, but the null-collapse case is gone, because "collapsed" would mean zero factors on screen. This is the one deliberate interaction change in the wave, and it is the change the brief asked for. Rejected: keeping the toggle with a fallback to the top task, which makes clicking the top row do nothing visible. |
| D-109 | Each factor bar draws the **contribution against that factor's own weight** as a faint ceiling behind it, both scaled to the largest weight in the set | Normalising each bar to its own weight would make a factor with weight 0.05 and full value look identical to one with weight 0.20 — the weights would be on screen and the bars would contradict them. Scaling to the set's maximum makes lengths comparable across rows, and the visible gap between the faint segment and the solid one *is* the unrealised headroom, which is the thing a reader wants when they are about to move a weight. |
| D-110 | Bars in the factor table and the ranked list take the task's **band** colour through `severityFill`; the exposure bar stops using the accent | This is the shared mapping used as intended rather than a fourth palette. The side effect is the useful one: seventeen ranked rows read red → amber → grey top to bottom, so the band distribution is legible without reading a single number, and the accent is freed for the critical path. |
| D-111 | Disclosures are native `<details>`/`<summary>`, not a re-implementation of the old `Disclose` | `Disclose` was on the stop-using list and `ui.tsx` was not the panels' to edit, which left a choice between duplicating a twelve-line component in three files and using the element the platform already has. `<details>` needs no state, no client boundary and no component, and `ErrorBoundary` was already using it — so this is convergence, not a new pattern. The marker needs `list-none`, `marker:content-none` *and* the WebKit pseudo-element to disappear in Chrome. |
| D-112 | Callouts and empty states are token markup, **not** shadcn's `Alert` / `Empty` | The skill's rule is to use those components and both would be correct — but neither is installed, and installing them means writing new files into `src/components/ui/` in a working tree three agents were editing simultaneously. A shared-primitive collision costs everyone; a hand-built block in a file you own costs nobody. Worth revisiting once the tree is quiet. |
| D-113 | Severity and band chips take their **shape** from the `Badge` primitive and their **colour** from `severity.ts`, overriding the variant's colours via `className` | The shadcn rule against overriding a component's colours and the repo's rule that severity colours come from one shared mapping are in direct conflict for exactly this element. Keeping the one with a correctness consequence — three states, one implementation, no per-panel drift — and breaking the stylistic one is the right way round. If it ever needs to be clean, the home is a `severity` variant in `badge.tsx`. |
| D-114 | `ErrorBoundary` stays a class component; only the fallback markup changed | Checked rather than assumed, because this was the file most likely to have moved under us: Next 16.3.4's own error-handling reference still documents `getDerivedStateFromError` plus `componentDidCatch` on a class, and there is still no hook equivalent. The `resetKey` clear in `componentDidUpdate` is byte-identical, and D-68's property was re-verified by forcing a render throw in one panel and confirming the panels above and below it kept rendering. |
| D-115 | The analysis stage is **one primary surface and a narrow inspector rail**: the dependency map first and full width in the main column, the findings list beneath it, and the projected-finish numbers plus the narration in a 288px rail | The brief asks for a hero and "not three equal columns". Both things in the main column genuinely need the width — a dense finding list and a time-axis chart — so the rail is the thing that gets narrow, and the numbers are the right thing to put in it because they are what you glance at rather than what you work in. It collapses to a single column below `lg`, main column first, so a narrow window degrades to reading order rather than to a squeezed chart. The cost is real and was measured: at 1600px the rail takes ~300px off the chart, which is why the map now fits its container width instead of using a fixed scale. |
| D-116 | The four headline numbers are **one dense typographic line**, not a four-up tile grid | A box around each number says all four matter equally, and they do not: the projected finish is the answer and the slip is why anyone cares, so those two carry the size and the colour while the planned finish and the deadline verdict read as context. This was the literal instance of "the uniform card-grid layout where every tile has equal visual weight" that the brief names, so removing it was not a matter of taste. "Projected finish" survives as literal visible text because the walkthrough matches it and because it is the label a reader scans for. |
| D-117 | The reserved accent is taken **off the primary button, the loading spinner and the disclosure triggers** in the shared primitives | `ui.tsx`'s `Button variant="primary"` was `bg-accent`, so every primary action in the product was accent-coloured — which reserves the accent for nothing. It is now `bg-primary`, the strong neutral D-94 defined it as. Same reasoning for the spinner and the tier-banner disclosure: a loading indicator and a navigation affordance are not meaning. One change in one file fixed every consumer at once, which is the argument for the shared primitives having survived wave 1 unedited. |
| D-118 | `TierBanner` and `Assumptions` become **hairline rules instead of boxes**, and keep every word | They were the last two boxed surfaces on the analysis stages, and they are also the two components that carry product commitments rather than styling — which is exactly why the change had to be to the frame and not the contents. Every sentence, the unavailable-checks list, the per-tier reasons and the "what would make this a probability" block are byte-identical; they now sit under a rule rather than inside a border. Centralising it in `ui.tsx` rather than asking three agents to each restyle their own copy is what kept the analysis and risk stages looking like one product. |
| D-119 | The dependency map is a **time × resource-lane timeline**, not a node graph. React Flow is kept as the canvas; dagre is deleted | The node graph answered "what depends on what" and nothing else — no *when*, and no *who is doubly booked*, which are the two questions a workflow owner actually has. Every input the lane view needs was already in the analysis payload (`es`, `ef`, `lf`, `slack`, `critical`, assignee labels that match `resources[].label`, `today_day`), so this is a layout change and not a data change. On a time axis, resource contention is visible without a finding having to name it. React Flow stays because it renders the edges, the pan/zoom and the day grid for free, and because `journey.mjs` asserts `.react-flow` is present — replacing it would have been a second, larger change hidden inside the first. Rejected: hand-rolled SVG, which buys nothing and breaks that assertion. |
| D-120 | Pixels per day is **derived from the container** (usable width ÷ horizon, re-measured with a `ResizeObserver`), clamped 3–44 px/day, with the tick interval chosen from a nice-number ladder | A fixed 28 px/day fitted while the panel was full-bleed and clipped the projected end — the number the chart exists to show — the moment the inspector rail of D-115 took 288 px away. A chart whose x-axis is time has to show all of the time by default; zoom should be opt-in, not the price of reading it. `ResizeObserver` rather than a window listener, because a rail appearing is not a window resize. Verified at two viewport widths on both fixtures, which also revealed the clip was never width-dependent: the main column is `max-w`-bounded, so it was purely the fixed scale. Rejected: `fitView` on mount and resize, which keeps a natural scale nobody chose and tucks the lane gutter under the fitted bounding box. |
| D-121 | Bars are **stacked into sub-rows** within a lane when their work windows overlap, and the lane label spans them | Drawing two concurrent bars at the same height made them overlap into unreadable mush on exactly the fixture where contention is the point. Packing on `es…ef` means a lane is one row deep until the schedule genuinely asks a resource for two things at once, and then it is two — the same fact the engine reports as `resource_overallocated`, shown rather than asserted. Packing on the *full* extent including float was rejected: it separates bars that never collide in work, and turns "doing two things at once" into "might one day overlap". |
| D-122 | Slack is **drawn, not coloured**: a strip on the bar's baseline running to `lf`. The near-critical amber tier is deleted | The old graph spent three colours on slack — red critical, amber ≤2 days, neutral otherwise — which is a rainbow standing in for a number the data already carries exactly. A float strip says *how much* room a task has, which amber never could, and it frees the accent to mean only the zero-slack chain. It was first drawn as a mid-height rule, which read as a dependency edge crossing the lane; moving it to the baseline and making it opaque fixed that. |
| D-123 | Bars are **two layers** — an opaque shell with a tinted inner fill | React Flow draws edges beneath nodes, so a translucent bar let an edge show through and appear to strike out the task's own label. An opaque shell puts the edge behind the bar where the z-order already intended it. The alternative, a `color-mix` in an arbitrary Tailwind value, does the same thing in a form nobody can grep for. |
| D-124 | Table cells hold **real inputs drawn without their borders** until hover or focus — a deliberate divergence from the shadcn `Input` default | Seventeen rows of outlined boxes is the "generated form" look this pass is written against, and the point of the work table is that it reads as data you can type into. The behaviour is untouched: same input, same `onBlur` patch, same request. Dark mode needed `dark:bg-transparent` stated explicitly, because `Input`'s own `dark:bg-input/30` otherwise wins and every cell comes back as a filled box — found by looking at a screenshot, not by reading the class list. |
| D-125 | Every select in the builder becomes a Radix `Select` with a `"__none__"` sentinel for "nothing chosen" | Radix refuses `""` as an item value and the native selects used `""` for exactly that. The sentinel never leaves the component — state still holds `""` — so `createDependency`, `addAssignment` and `createProject` receive byte-identical bodies. Safe only because `journey.mjs`'s `locator("select")` indexing runs on the what-if stage, where neither the builder nor the member list is mounted; that was checked in `page.tsx` before the conversion, and it is why D-104 reaches the opposite conclusion for the what-if panel's own selects. Two panels, two answers, one reason: what the walkthrough can actually reach. |
| D-126 | The roles copy in `SetupPanel` is **corrected and promoted**, not softened, and states the three roles *and* that reads stay open | "Roles are advisory" was true when written and false since the guard landed, and it appeared only in the empty state — so a project that had members showed no role claim at all. The replacement is read off `deps.py` rather than paraphrased, and it carries D-85's other half, because "roles are enforced" without "reads stay open to anyone signed in" over-claims in the opposite direction. Same discipline as D-91: a copy line that has become false gets corrected, not quietly dropped. |
| D-127 | `VersionHistory` shows the **real instant to the second, labelled UTC**, and still shows **no author** | The brief asked for "when" and "who". `created_at` always existed on `WorkflowVersion` and was simply never projected, so exposing it was a projection and not a migration — and once exposed it belongs on the row at full precision, because a relative form ("3 days ago") is a rounded impression and this is the one panel whose entire job is provenance. It is parsed as UTC even though SQLite returns the value with no offset, because reading it as local time would shift every timestamp in the panel by the viewer's own offset, silently and differently for each reader — verified by rendering it in `Asia/Kolkata` and checking the instant came back unchanged. "Who" stays absent: there is no author column at all, so it needs a schema migration, and inventing one client-side would be a fabricated fact in the panel least able to afford one. The file's caveat was narrowed to the half still true rather than deleted once the other half was fixed. |
| D-128 | `violet` is gone from the builder panels too, so the token can finally be deleted | A constraint identifier is an identifier on record, not a fourth severity, and giving it a hue next to three severity states is what made the palette read as a rainbow. Both wave-2 halves independently landed on distinguishing it by *form* — a bordered mono chip on the inset surface — which closes the open half of D-97: `violet` survived the design-system pass only because components still named it, and now none do. |

---

## Phase 11, wave 2 — capability

| # | Decision | Reasoning |
|---|---|---|
| D-129 | The four new routers' **route templates were declared up front**, returning 501, and `main.py` / `deps.py` / `test_auth.py` were wired against them in one commit *before* the agents ran | All four agents needed those three files. Four concurrent edits to the role-guard exemption list is how a write route silently becomes viewer-accessible, and the file that would have caught it is the same file being raced on. Declaring the contract first cost one commit and removed the class of failure entirely; it also fixed the URLs the frontend will be written against before anything depended on them. The exemptions carry reasoning rather than plumbing: replay, forecast and requirement-change are reads over an immutable snapshot, so they sit with `analyze` and `what-if`; `requirements/{key}/apply` is deliberately absent because it writes a version. |
| D-130 | **Beta-PERT (λ=4)** as the distribution family, fitted exactly to the three points a task already carries | `α = 1 + 4(m−o)/(p−o)`, `β = 1 + 4(p−m)/(p−o)`, sampled as `o + (p−o)·Beta(α,β)`, so the mean is `(o+4m+p)/6` and the mode sits at `likely`. Rejected **triangular** — also exactly determined by three points, but it puts far too much mass near `o` and `p`, the two numbers an estimator states least confidently. Rejected **lognormal** — models the real long right tail better, but is unbounded above and cannot be fitted from three points without inventing a parameter. The cost of the choice is stated in the payload rather than hidden: Beta-PERT is bounded above by the pessimistic estimate, so no simulated run can exceed the worst case somebody wrote down, and λ=4 is a convention, not a measurement. |
| D-131 | **numpy was not added.** The Monte Carlo kernel is vectorised column-major in the standard library instead | The phase brief said "vectorise with numpy"; `test_core_purity.py` pins `ALLOWED_THIRD_PARTY = {"networkx"}`, so any numpy import under `core/` fails a structural purity test, and the same brief lists "`core/` stays pure, the purity test must keep passing" as an absolute constraint. When an implementation instruction collides with an absolute constraint, the constraint wins. Measured: 5,000 iterations over 40 tasks and 75 dependencies in **0.267 s median**, against a ~2 s budget — so relaxing the allowlist would have weakened a real structural guarantee to buy nothing. One `map()` pass per graph edge over a 2,048-iteration chunk, so peak memory is independent of iteration count. |
| D-132 | Sampling rescales each task's band onto the engine's **observed** durations, not its planned ones, exactly as `feasibility.three_point_range` already does | A task already running over keeps its overrun in every sampled run, so P50 reconciles with the headline deterministic projection instead of contradicting it on screen (seeded fixture: P50 = 26.00 against a projected end of 26.0). A forecast whose median disagrees with the number printed beside it is worse than no forecast. |
| D-133 | **Three spread provenances, per task, never averaged**: `three_point_estimate`, `spread_prior` (labelled `assumed`), and `measured_actual` for completed work, which is held constant rather than sampled | Sampling a finished task's duration invents uncertainty about something that already happened. Verified on an imported project, where the only two non-assumed tasks are precisely the two that are `done`. |
| D-134 | The domain's variance prior reaches the engine as a **float on `EngineConfig`**, never as a domain name | The rule the whole engine is built on. A test asserts that two configs differing only in the provenance *label* forecast identically, so `test_domain_leak.py`'s guarantee extends to the new module by construction. |
| D-135 | Percentiles by linear interpolation between order statistics | P50 ≤ P80 ≤ P90 becomes a property of the function rather than a hope about the data — it holds on empty, single-task and zero-effort workflows too. |
| D-136 | Nothing to sample means **no distribution**: `available: false`, the reason, and `fall_back_to: "structural_estimate"` | A workflow with no three-point estimates anywhere and a zero prior would otherwise produce a spike on a single day dressed up as a forecast. The Layer-A structural score remains the cold-start answer, and the response always says which of the two it is reporting. |
| D-137 | **SSE, not WebSocket**, and the replay's controls are ordinary JSON over POST rather than a second channel | The traffic is one-directional and SSE survives a reverse proxy unconfigured. Putting pause/seek/speed on normal POST routes means they inherit the existing role guard, error envelope and request-id logging instead of needing a second copy of all three. |
| D-138 | `speed` is **simulated days per real minute**, and both derived units are echoed in every state payload | The contract's default of 60 therefore means one simulated day per second, which replays a three-week project in about twenty seconds — the pace a demo wants. Echoing `simulated_days_per_minute` *and* `seconds_per_simulated_day` means the unit is never inferred from a bare number. |
| D-139 | The replay steps on **every whole simulated day**, not only on days an event occurs | Several findings cross their threshold on the clock — `ready_but_idle` fires after `idle_threshold` days of readiness, and nothing happens on the day it crosses. Stepping only on events would report those late and make "findings appear at the correct simulated time" false. |
| D-140 | Stored `actual_durations` are **excluded** from reconstruction; observed durations come from the event log and the simulated clock only | They describe the present. Feeding them to a simulated past would let a frame at day 5 know what happened on day 20. In-flight work is still stretched by the engine's own observed-duration bridge, which is evidence that genuinely existed on that day. Stated in every frame's `derived.caveats`. |
| D-141 | **One replay per project, N subscribers fanning out.** `POST /replay` on a running replay replaces it and tells the old subscribers | Two viewers on two different clocks for the same screen is not a feature. A viewer arriving mid-replay gets a `catchup` frame computed synchronously at start, so nobody ever sees an empty screen — the failure this whole feature exists to prevent. |
| D-142 | **No `incremental.py`.** Full re-evaluation per step, and the file does not exist | The brief permitted incremental evaluation only with a proof of equivalence at every step. Full evaluation is ~24 ms against a step budget of 50 ms or more at demo speed, so the optimisation is unmeasurable, and equivalence across the whole detector suite — suppression, tiering, unavailable-checks — could not be proven. The simple correct thing wins, and `core/` is untouched by this feature. The finding *diff* lives in `services/` because diffing two serialised evaluations is presentation, not engine. |
| D-143 | Replays live in **process memory**, keyed by project id, lock-free, reaped after a 30-second idle grace period | A replay is a viewing position over already-durable data — scratch paper, exactly as a `Scenario` is. Persisting a cursor and a speed multiplier would put a write path next to a feature whose entire promise is that it writes nothing. The consequence is real and is recorded in `docs/DEPLOY.md`: **this feature requires a single process**, because `POST /replay` and `GET /stream` landing on different workers would not find each other. The grace period exists because `POST /replay` legitimately precedes the first `GET /stream` by a round trip, and a viewer refreshing the page must not destroy a replay other people are watching. |
| D-144 | A slow subscriber is **told**: a queue over 512 frames drops oldest and stamps `dropped_frames` on the next message | Dropping silently would let a viewer's screen disagree with the engine and never say why, which is the same failure mode as an unlabelled assumption. |
| D-145 | CSV arrives as `csv_text` in a **JSON body**, not a multipart upload | `python-multipart` is not installed, and adding a dependency was not this agent's call to make. It is also the better design here: preview becomes genuinely stateless, so `commit` re-posts the same body rather than trusting server-side state left by an earlier call, and the browser side is one `FileReader`. Bounded at 12 MB body, 5 MB CSV, 5,000 rows. |
| D-146 | Import request bodies are **read by hand against a ceiling**, not declared as typed parameters | FastAPI parses the body *before* solving dependencies, so no `Depends` can get in front of a 500 MB POST. The routes take `Request`, stream against a limit, then validate the same pydantic models manually and re-raise as `RequestValidationError` so `main.py` still renders the usual `{invalid_fields: [...]}`. `openapi_extra` preserves the documented schema, so `/openapi.json` is unchanged. |
| D-147 | **Absent and present-but-unreadable are different things.** A missing estimate is defaulted and labelled `assumed`; a `TBD`, a negative number or an unreadable date **rejects the row** | Defaulting an unreadable value launders a typo into a schedule. Rejected rows are reported with row number, raw content and reason, and are counted at the top of both the preview and the commit response — so committing cannot become a way to stop seeing them. |
| D-148 | All-numeric slash dates are **refused as ambiguous**, and `Original Estimate` is read as **seconds** by default with no sniffing | `03/04/2026` is two different days on two sides of an ocean. A unit heuristic that reads `8` as eight seconds and `28800` as eight hours will one day read a real number wrong and never mention it; an explicit default with an override cannot. |
| D-149 | **Every imported dependency is `consumes=false`** | A tracker's "blocks" link says nothing about artifact consumption. Guessing would make the requirement-change report claim `must_redo` on no evidence — the one place in this product where a fabricated edge does the most damage. Stated in the import's assumptions, along with the fact that marking consuming edges by hand afterwards is how you get it back. |
| D-150 | Rejected rows do **not** block commit; a cycle or an empty result does | A real export usually carries a few bad rows, and refusing the other four hundred would make the feature useless. A cycle is different in kind: it produces a workflow that cannot be scheduled at all, so it is named at preview — with the task-key path and the rows that caused it. |
| D-151 | **Commit never merges into an existing workflow.** There is no `project_id` on the body, and a test asserts its absence | "Import into a live workflow" is the operation with no undo. Owner handling is copied line-for-line from `projects.py`, and a test compares a hand-made project against an imported one to prove the two are indistinguishable. |
| D-152 | The importer **invents no history**. Statuses come across; the event log starts empty | So an imported project honestly reports a lower evidence tier and a non-empty `unavailable_checks`, rather than carrying a fabricated backdated log that would make the Tier-2 detectors look available when they are not. Asserted. |
| D-153 | A webhook resolves its project from **`?project_id=` in the URL**, and its task by matching a project task key as a whole token in title, then head branch, then body | Configuring the webhook URL *is* a repository admin saying "this repo is that project". Inferring it from `repository.full_name` would let anyone who can guess a repo name pick your project. Resolving to neither returns **202, not an error** — most repository activity is about work the workflow does not track — and no task is ever invented. |
| D-154 | Webhook **redelivery is a no-op with no idempotency table**: `from_status` is read from the project's own event log, so a second delivery finds `from == to` and appends nothing | The event log is a log of *transitions*, so deduplication falls out of the data model instead of needing a table beside it. `edited`, `labeled`, `assigned` and `synchronize` are ignored at 202 for the same reason: they are not transitions, and feeding them in would be noise in the evidence the Tier-2 detectors reason from. |
| D-155 | `requirement_revisions` is **project-scoped, not version-scoped**, and stores the full text on every row | `WorkflowVersion` copies every row on every bump, so version-scoped history would be re-copied whenever somebody edited an unrelated task, and authorship would be re-stamped by the copy. History is about what happened and outlives versions — the identical argument `Event` already makes for itself. Full text rather than a delta because the strings are at most 2 KB, and "diff v1 against v3" becomes two lookups instead of a replay that can silently reconstruct a wording nobody ever wrote. `consumed_by_task_keys` is copied onto the row so a historical impact is computed against the consumption set that existed *then*, with a `consumption_drifted` flag when it no longer matches. |
| D-156 | The replan is expressed in the **existing seventeen mutation kinds**; no eighteenth was added | An unscoped requirement change is exactly one `REQUIREMENT_VERSION_BUMP`, whose applier already carries the whole semantics. A *scoped* change is that bump followed by `TASK_STATUS_SET` mutations restoring the spared statuses — which is literally what the decision was, so the scenario reads as the decision. Everything goes through `scenarios.create` then `evaluate_scenario`, and apply promotes via `apply_scenario`: there is no second write path. |
| D-157 | **`schedule_impact.delta_days` is usually 0, and the payload says plainly that this is not the change being free** | `cpm.py` contains no reference to task status — the scheduler is status-blind, so a completed task already occupies its full duration in the projection and re-opening it cannot lengthen the critical path. On the arithmetic fixture, 7 days of effort are genuinely lost and re-spent while the projected finish does not move. Rejected: a second scheduler that compresses completed work out of the remaining plan, which would change what `analyze` means for all four capabilities — forbidden by this phase's own constraint against touching `core/` semantics, and far too large to do quietly inside a requirement report. So the report emits `rework_shows_as_calendar_slip: false`, a caveat on the block, that caveat appended to the headline statement, and an `unavailable` entry naming exactly what would unlock it. **The consequence for the UI is that the requirement screen leads with the effort figure, not the date**, and the demo script is corrected to match rather than the number being made to look better. |
| D-158 | Comparing two plain wordings of the same requirement returns an **explicit tie**, with `cheapest_option_index: null` and a sentence saying why | Two wordings have identical graph-derived cost, because the blast radius comes from the dependency graph and the graph does not change when the sentence does. Deriving a difference from how much of the text moved would have been an invented number in the one feature whose whole claim is that it reasons from evidence. A comparison becomes real when the options declare which consumers each one spares, so the endpoint also accepts `{text, invalidates}` and 422s when `invalidates` names a task that never consumed the requirement. |
| D-159 | `stale_tasks` keeps its exact signature and behaviour; `stale_report` **calls** it rather than reimplementing the walk | The two can therefore never drift, and a test asserts they agree across every seeded requirement. The additions to `staleness.py` are 124 lines with zero deletions, and the purity test passes unchanged. |

---

## Phase 11, wave 3 — the screens

| # | Decision | Reasoning |
|---|---|---|
| D-160 | `src/lib/api.ts`'s Phase 11 block and `page.tsx`'s stages were written **before** the four UI agents ran, from payloads captured against a running backend | D-129 one layer up. All four agents needed the client, and four concurrent edits to it is how two panels end up calling one endpoint two different ways. Capturing real responses rather than transcribing the backend agents' reports was the right instinct and still not enough: three types were wrong at nested level — `OwnerImpact.must_redo` held task objects rather than keys, the comparison's option field is `impact` not `report` with its sentence on `differences.statement`, revisions use `recorded_at`/`consumed_by`, and `apply` returns neither `version_id` nor `version_no`. All three were found by agents building against them, and the last would have shipped a banner reading "Version undefined was written". Capture nested shapes, not just top-level keys. |
| D-161 | Each new stage has **one mount point**, and the agent composes its own children behind it | `LiveFeed` owns Clock, ReplayControls and the map; `RequirementChange` owns ImpactReport and RequirementHistory. The alternative — `page.tsx` mounting four siblings that share replay state — would have put a shared hook across a file-ownership boundary, which is the coupling most likely to go wrong between two agents who cannot see each other's work. |
| D-162 | `assumptionSentences()` and `unavailableEntries()` render an assumptions block by **iterating** it, never by reading known keys | If the honesty layer needed a type update to be visible, the day someone adds a caveat is the day it silently stops showing. Verified in practice: scoping a requirement change makes the backend emit two extra caveats and both appeared on screen with no frontend change. Two panels found the function structurally insufficient for their block's shape and extended it in kind rather than reverting to key lookup — `ForecastPanel`'s `flatten()` picks up booleans and counts a string filter cannot see, and `ImportPanel`'s `AssumptionsView` walks shapes. Both subtract lifted sentences **by key**, so a rename shows a sentence twice rather than hiding it. |
| D-163 | SSE reaches the browser through `EventSource`, and that it survives the Next proxy was **measured before any agent built on it**, not reasoned about | `NextResponse.rewrite` claims to hand off to Next's streaming proxy; buffering would not have errored, it would have left the live screen empty and then dumped everything at once — the failure you discover on stage. Real browser, real session, real proxy: `catchup` at 19ms, then a frame every ~265ms spread over 3.45s. `EventSource` over `fetch`+reader because it is same-origin so the session cookie rides along, and it reconnects by itself. |
| D-164 | The live stage probes `GET /replay` and **starts one only on a 404** | Probing before starting is what lets a second viewer *join* a running replay rather than restarting it under the first, because `POST /replay` on a live replay replaces it (D-141). The cost is a browser-logged 404 on every cold arrival. Rejected changing the route to answer 200-with-`running:false`: it is a defensible shape, but it would rewrite a contract three things already build against to remove a cosmetic console line. Instead `journey.mjs` now records each console error's URL alongside its text — Chromium's message for a failed request names no URL, so a filter written against the text alone can only exempt *every* 404 or none — and exempts exactly this one. |
| D-165 | The clock **does not interpolate** between frames | Interpolating would put a simulated day on screen that the engine never evaluated, immediately next to a projection computed at a different day. The clock steps when a frame lands, and `journey.mjs` asserts it advances on its own, because a screenshot cannot tell a running replay from a stopped one. |
| D-166 | The requirement screen **leads with wasted effort, not the date**, and `+0d` can never appear alone while completed days are lost | The direct consequence of D-157, and a correction to this phase's own brief, which scripted the line *"Finish date moves from 23 Sep to 27 Sep"*. It will not, for most changes. The date row and its caveat are one indivisible block, with a written fallback for the case where the backend sends an empty caveat because nothing was actually wasted. |
| D-167 | A **scoped** report says so on the headline, beside the number | Scoping is the one judgement the system cannot make, and a scoped report otherwise produces a small, cheap-looking number that is screenshot-indistinguishable from an unscoped one. The badge gates on `scoped_out.length > 0` rather than on `scoped`, because a scope that spares nobody removes nothing and a badge there would claim a judgement nobody made. |
| D-168 | The forecast panel renders **exactly one answer**, chosen by `answer_kind`, and its disambiguation table contains **no figures** | Rejected showing both numbers with a warning: a warning is a request, a missing code path is a guarantee. The opening table names each number, its scale and where it lives — a table with no values in it cannot be misread as a comparison of values. `BandLabel` takes the number as a *required prop* printed inside the same badge, so "a band is never shown without its number" is a type error rather than a habit. |
| D-169 | `color-scheme` is set on the theme in `globals.css`, not per input | Nothing set it, so Chromium painted every native widget for a light page and the date inputs' calendar glyph came out black on the near-black canvas. Two agents had independently grown two different per-input workarounds in two files. It is a property of the theme, not of an input, and one block fixes every native control — pickers, scrollbars, focus rings — app-wide. |
| D-170 | The backend's `no_impact` sentence now **branches on `scoped`** | Two very different situations produce identical zeros: "nothing consumes this requirement" is a fact about the workflow, "you scoped every consumer out" is a report of the reader's own judgement. Asserting the first when the second is true tells someone their requirement is free to change when what actually happened is that they said so themselves. Found by the UI agent, which had worked around it on screen; the fix belongs in the sentence. |
| D-171 | Native `<select>` stays in the what-if composer and the optimizer budget; the new panels use Radix where no walkthrough reaches them | D-104 and D-125 unchanged, and re-verified rather than assumed: `journey.mjs`'s `locator("select").nth(1)` indexing was replayed three times against the untouched controls. `ImportPanel` mounts only on the project picker and `ReplayControls` only on the live stage, so both take Radix. Two panels, two answers, one reason — what the walkthrough can actually reach. |
| D-172 | The risk stage is labelled **"Risk & forecast"**, not "Forecast" | The stage carries both numbers and a label naming only one misrepresents what is on it. The original rename broke `journey.mjs`'s selector, which three separate agents correctly reported as not theirs; the fix was to correct the label *and* the selector rather than to make the test match a name that had become inaccurate. |
